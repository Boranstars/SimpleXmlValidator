# 模块实施计划

> 本文件是模块实施与验收清单。具体设计和接口语义以 `docs/概要设计报告.md` 为准。

## 当前状态

- 当前阶段：阶段 1
- 阶段状态：待 Claude 修正
- 当前交付：输入检查模块的规范化路径输出
- 最后更新：2026-07-15

## 协作职责

| 角色 | 职责 |
|---|---|
| Claude | 按本计划和项目规范实现生产代码、更新构建文件与必要开发文档 |
| Codex | 检查阶段实现是否符合设计、编写和维护 GTest、运行验证并更新本计划状态 |

Codex 不负责实现本计划中的生产代码；发现实现偏离设计时，应记录问题并交由 Claude 修正。

## 目标目录与文件

```text
src/
  core/
    validation/
      ValidationResult.h
      InputValidator.h/.cpp
      XmlValidator.h/.cpp
  infrastructure/
    xerces/
      XercesRuntime.h/.cpp
      XercesErrorHandler.h/.cpp
      XercesSchemaValidator.h/.cpp
    logging/
      Logger.h/.cpp
  gui/
    MainWindow.h/.cpp
    ValidationResultPresenter.h/.cpp
tests/
  core/validation/
    InputValidatorTest.cpp
    XmlValidatorTest.cpp
  infrastructure/xerces/
    XercesErrorHandlerTest.cpp
    XercesSchemaValidatorTest.cpp
```

仅在实现确有需要时增加文件；新增或调整文件时必须同步更新本节、`CMakeLists.txt` 和对应测试。

## 构建目标建议

推荐先将核心校验能力编译为静态库，而不是让 GUI 目标直接编译所有校验源文件：

```text
SimpleXmlValidatorCore  # STATIC：core/validation + infrastructure
SimpleXmlValidator      # GUI 可执行程序：gui + main.cpp，链接 Core
SimpleXmlValidatorTests # GTest：链接 Core，不依赖 GUI
```

- `SimpleXmlValidatorCore` 对外暴露 `core/validation` 的公共头文件，内部链接 Xerces-C++ 和 spdlog。
- `SimpleXmlValidator` 只链接 Qt 和 `SimpleXmlValidatorCore`，不得直接链接或包含 Xerces 头文件。
- `SimpleXmlValidatorTests` 链接 `SimpleXmlValidatorCore` 与 GTest，复用同一套生产校验逻辑。
- 当前不建议额外拆出“纯 core 库”和“infrastructure 库”；项目规模较小时会增加 CMake 和依赖管理复杂度。若未来需要替换 Xerces 或提供其他后端，再引入接口库或拆分目标。

## 阶段清单

### 阶段 1：核心模型与输入检查

- 状态：待 Claude 修正
- Claude 交付：`src/core/validation/ValidationResult.h`、`src/core/validation/InputValidator.h`、`src/core/validation/InputValidator.cpp`。
- Codex 测试：`tests/core/validation/InputValidatorTest.cpp`。
- 验收：路径为空、不存在、目录、不可读、空文件和有效的中文/空格路径均返回符合约定的结果；有效路径必须为绝对且词法规范化的路径，不保留 `.`、`..` 或冗余分隔符。
- 验收记录：2026-07-15，规范化路径契约新增后共运行 15 项输入检查测试，其中 12 项通过，3 项失败（相对路径、`..` 路径、工作目录变化后的相对路径）；待 Claude 在文件状态检查前完成路径词法规范化后重新验收。

### 阶段 2：Xerces 基础封装

- 状态：未开始
- Claude 交付：`src/infrastructure/xerces/XercesRuntime.h/.cpp`、`src/infrastructure/xerces/XercesErrorHandler.h/.cpp`。
- Codex 测试：`tests/infrastructure/xerces/XercesErrorHandlerTest.cpp`。
- 验收：Xerces 初始化和异常被封装；`warning`、`error`、`fatalError` 可转换为项目内部错误结构，且不依赖 Qt。

### 阶段 3：XML/XSD 校验流程

- 状态：未开始
- Claude 交付：`src/infrastructure/xerces/XercesSchemaValidator.h/.cpp`、`src/core/validation/XmlValidator.h/.cpp`。
- Codex 测试：`tests/core/validation/XmlValidatorTest.cpp`、`tests/infrastructure/xerces/XercesSchemaValidatorTest.cpp`。
- 验收：先确认 XSD，再校验 XML；合法 XML 返回 `Valid`，XML 内容错误返回带行列号的 `Invalid`，无效 XSD/依赖缺失返回 `Failed`。

### 阶段 4：日志与构建整理

- 状态：未开始
- Claude 交付：`src/infrastructure/logging/Logger.h/.cpp`，以及 `CMakeLists.txt` 的核心库、GUI 和测试目标调整。
- Codex 检查：验证日志写入失败不改变校验结果；执行受影响的 GTest、CMake 配置和构建。
- 验收：核心测试不依赖 Qt GUI；Xerces、spdlog 和 GTest 仅由需要它们的目标链接。

### 阶段 5：GUI 接入与展示

- 状态：未开始
- Claude 交付：`src/gui/MainWindow.h/.cpp`、`src/gui/ValidationResultPresenter.h/.cpp`，并更新 `main.cpp`。
- Codex 检查：核对按钮状态、成功/失败提示和表格列；必要时为可脱离 GUI 的展示逻辑补充测试。
- 验收：GUI 不直接使用 Xerces；`Valid`、`Invalid`、`Failed` 的展示符合概要设计。

### 阶段 6：集成验证

- 状态：未开始
- Claude 交付：修复验收发现的生产代码问题及必要构建问题。
- Codex 测试：补齐回归测试，运行完整 GTest、CMake 构建和现有 XML/XSD 样例验证。
- 验收：覆盖合法 XML、格式不良好、结构不匹配、必填项缺失、类型/枚举错误、输入异常、无效 XSD 与 XSD 依赖加载失败。

## 状态更新规则

- Claude 开始实现前，将对应阶段改为“实现中”；提交交付后改为“待 Codex 验收”。
- Codex 完成检查和测试后，改为“已完成”；若失败，改为“待 Claude 修正”并在该阶段追加问题说明。
- 任何接口语义或范围调整，先更新 `docs/概要设计报告.md`，再同步本计划与测试。
